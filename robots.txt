# Not the deployed robots file. scripts/build.py writes robots.txt into each
# artifact: staging allows crawling but every page carries noindex, production
# names the production sitemap. See _site/README.md.
User-agent: *
Allow: /
